# Backend API URL - set this to your FastAPI backend URL
# This variable must be prefixed with NEXT_PUBLIC_ for client-side access in Next.js.
# The /api prefix is appended automatically by the API client.
# Required: Set this to the backend preview URL (e.g., https://your-backend-host:3001)
NEXT_PUBLIC_API_URL=http://localhost:3001
